# Database connection string for PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/aeroscan?schema=public"

# WebSocket server URL (use ws:// or wss:// depending on your environment)
NEXT_PUBLIC_WS_URL="ws://localhost:5555"

# Private key for signing transactions (never commit the real key!)
PRIVATE_KEY="your-private-key-here"

# Helius RPC URL for Solana (Mainnet/Devnet endpoint)
HELIUS_RPC_URL="https://rpc.helius.xyz/?api-key=your-api-key"

# Token for authentication/authorization
MC_TOKEN="your-mc-token-here"
